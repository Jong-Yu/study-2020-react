export function header() {
  // console.log('저는 헤더 입니다')
}

// export default header
// export default == function
// export == function 을 객체로 감싸서 보냄

// import 할 때 객체를 { 객체 } 로 감싸면 obj.header이 아닌 header 이름으로 사용 가능
